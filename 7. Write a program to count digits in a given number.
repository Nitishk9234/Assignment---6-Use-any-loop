#include <stdio.h>
#include <conio.h>

int main()
{
     int count=0,n;
     printf("Enter a N value :");
     scanf("%d",&n);
    while(n!=0)
    {
        n=n/10;
        count++;
    }
    printf("Number of digits is %d",count);

    getch();
}
